name: NodeJS with Grunt

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 'Hello World'
      run: echo "Hello World"
    - name: 'Dime la fecha'
      run: date
    - name : Muestra el contenido del Escritorio
      run : ls -la
